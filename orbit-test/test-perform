#!/usr/bin/python1.5

import CORBA
import sys,string
import Lightboard

orb = CORBA.ORB_init(sys.argv, CORBA.ORB_ID)
ior = sys.argv[1]
o = orb.string_to_object(ior)
l=o.getInstrument('foo')

print o, o.__repo_id,l

for x in range(1,1000):
  l.setAttribute('a','x')
